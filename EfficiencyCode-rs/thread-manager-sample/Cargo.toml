[package]
name = "EfficiencyCode-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single EfficiencyCode workspace dependency.
# Add new EfficiencyCode surface area to `EfficiencyCode-core-api` instead of depending on
# additional `EfficiencyCode-*` crates here.
EfficiencyCode-core-api = { workspace = true }
tracing = { workspace = true }
